# Linux Setup

This file is the future Linux-specific getting-started guide. For now, use the practical agent runbook in:

```text
docs/across_platform_agent.md
```

## Target Platform

Recommended first target:

```text
Ubuntu 22.04 LTS or Ubuntu 24.04 LTS
```

## TODO For Future Agent

Fill this guide with confirmed steps after running the Ubuntu setup test:

- Exact Ubuntu version tested.
- Docker Engine / Docker Compose install steps that worked.
- Whether NVIDIA GPU acceleration was tested.
- Whether headless Gazebo RTF improved.
- Whether noVNC/headed Gazebo works.
- Quest `adb reverse` behavior on Ubuntu.
- Unity APK install/rebuild path on Linux.
- Known Linux-specific failures and fixes.
